{
  "name": "openwifipassmap-web-assets",
  "private": true,
  "description": "Build pipeline for the server-rendered public web: compiles Tailwind+DaisyUI to internal/web/static/app.css and vendors Leaflet so all assets are same-origin (required for offline/PWA).",
  "scripts": {
    "build": "npm run build:css && npm run build:vendor",
    "build:css": "tailwindcss -i ./input.css -o ../internal/web/static/app.css --minify",
    "build:vendor": "node vendor.mjs",
    "watch": "tailwindcss -i ./input.css -o ../internal/web/static/app.css --watch"
  },
  "devDependencies": {
    "daisyui": "^4.12.14",
    "leaflet": "1.9.4",
    "leaflet.markercluster": "1.5.3",
    "tailwindcss": "^3.4.14"
  }
}
